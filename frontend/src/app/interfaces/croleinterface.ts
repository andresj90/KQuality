export interface CRole {
    success : boolean, 
    elements : [{
        id: number;
        name: string; 
        description: string;
        createdAt: string; 
        updatedAt: string; 
    }]
}